[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers
markers =
    slow: marks tests as slow (model loading required) — deselect with -m "not slow"

# Output
addopts = -v --tb=short
